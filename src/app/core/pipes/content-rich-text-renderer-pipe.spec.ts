import { ContentRichTextRendererPipe } from './content-rich-text-renderer-pipe';

describe('ContentRichTextRendererPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentRichTextRendererPipe();
    expect(pipe).toBeTruthy();
  });
});
